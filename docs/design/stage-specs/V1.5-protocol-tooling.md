# V1.5 阶段契约：确认协议工具化（ADR-009 落地）

> 立项 2026-08-31。前置：ADR-009 已 Accepted；调研四源已入库（`docs/design/research/01-04`）。
> 阶段推进沿 S0→S5 顺序，每阶段 push + CI 绿作下一阶段前置（E1）；验收 = 重构后 Mac 真机完整轮。

## S0 立项与 spike（本阶段）

DoD（可执行断言）：
- [ ] ADR-009 入库（`docs/decisions/009-protocol-tooling.md`）✅
- [ ] 本 spec 入库 ✅
- [ ] Spike-1 透传：dev 环境 gateway 注入 4 个协议工具定义 → 真实 Command Code 调用 10 轮 → 工具目录完整到达模型（无截断/限流）→ 结论记录本文附录
- [ ] Spike-2 采样：`propose_plan` 扁平 schema ×50 次真实调用 → args 合格率 ≥95% → 数据记录附录（若 <95%：schema 再扁平化或触发兜底加权）
- [ ] Spike-3 V4 非标参数：`thinking:{type}` + `extra_body.reasoning_effort` 请求体适配 PoC → 模型按预期开/关思考（对照 deep-code `openai-thinking.ts` 实现）

TDD 锚点：spike 脚本入 `scripts/spike/`（本机私有）——结论进本文件，脚本不入库。

## S1 协议工具定义与 gateway 拦截

DoD：
- [ ] `src/domain/protocolTools.ts`：四工具 schema 单源（扁平——无深嵌套数组套数组；字段级 zod/手写校验器 + 路径化错误消息模板「字段路径 + 期望类型 + 正确示例」）
- [ ] gateway：注册四虚拟工具；调用拦截 → L1 可测纯函数 `decideProtocolToolCall(state, tool, args)` → 三分支：置决策点（pending+decisionContent）/ 硬序拒绝（policy 错误回模型——approve-files 模式）/ 参数校验失败（错误模板回模型）
- [ ] 乱序矩阵 L1 锁定：plan 前 propose_goal 拒绝；completion 前 plan 未确认拒绝；重复 propose 同点拒绝（幂等/覆盖语义按 ADR-006）
- [ ] revision CAS：decisionContent 快照带 revision——模型基于过期快照的重复提议返回 INVALID_UPDATE（dsh 模式）

## S2 renderer 接线（卡渲染零改动验证）

DoD：
- [ ] 工具 args → decisionContent 快照（结构沿用现有 GoalProposal/PlanProposal/CompletionClaim——渲染层零改动）
- [ ] pending/decisionContent 派生路径 L3 断言：四卡（goal/plan/file-approval 转授权流/resolution）从工具调用达 pending 与原文本路径行为一致
- [ ] 文本标记路径降级：标记命中 → 不再直接置 pending，改为打 `protocol.text_fallback` 引导事件（模型被引导改用工具）——L1 锁定

## S3 sysPrompt 契约改写 + 兜底

DoD：
- [ ] sysPrompt ⑬⑭⑮ 重写：确认提议必须以工具调用发起；文本标记声明为兜底（降级通道兼容说明）
- [ ] candidates 文本协议迁移 `ask_user`（type 枚举含 approach_choice/risk_confirmation——deer-flow 类型集）
- [ ] sysPrompt 引导词 ⊆ isConfirmIntent 词表的机器校验（`sysPromptConfirmWords.test.ts`）保持通过

## S4 解析层退役 + 测试迁移

DoD：
- [ ] parsePlanProposal/parseCompletionClaim/goalFallbackTrigger 调用点全部降级为兜底探测（grep 清零直接产卡路径）
- [ ] scenarios.ts 场景助手工具化（四工具 chunk 形态）；L1/L3 全绿
- [ ] 覆盖矩阵更新（协议工具 ↔ 决策点 ↔ 断言三向）

## S5 真机验收（重构完成门）

DoD：
- [ ] Mac 新包（含 V1.5 全部）真机完整轮：0-1 全流程（澄清→目标→方案→approve→执行→【已达成】→解决）零解析 P1
- [ ] 五项复验（原 findings 尾部清单）+ 新增：乱序调用被引导、空卡不复现、文本兜底触发率打点
- [ ] 验收通过 → #6 关闭 + V1 收尾完成判定

## 附录：spike 结论（S0 执行后回填）

（待执行）
