# Stage S6 Spec（门控双维）

> 来源：docs/design/intent-confirmation-domain-design.md §6 S6 + §3.3 门控（sessionGate×actionGate——不变量 3）+ §8.1 A（授权执行层同源——main preApproval 改引用 classifyReadonly）+ 拍板 3（315 行：curl localhost 自动放行、外网 GET ask——安全默认；main preApproval 同步放行）。
> 状态：定稿（2026-08-16 开工）。
> 前置：S1 领域层（classifyReadonly 升级版——链递归/git 子命令/网络只读已实现；sessionGate×actionGate/canExecute 已实现——actionGate 含拍板 3 策略（localhost 自动/外网 ask）；classifyAction 兼容壳（内部已同源调 classifyReadonly——network-read 按 side-effect fail-closed——S1 安全保持）；S5 唯一推进判定器已就位（require-action/require-advance）。
> 本阶段核心：**actionGate 全量接线**——ask 语义进执行流（main preApproval 改引用 classifyReadonly + localhost 放行——拍板 3 落地 main 侧；renderer side-effect 判定统一领域层同源——坑 97）；classifyAction 兼容壳移除；冒烟门禁。

## DoD（机器可验证断言——stage-gate 逐条执行）

- [ ] L1 全量绿：`cd apps/desktop && npx vitest run`（新增 ≥10 条——见 TDD 网格）
- [ ] L2 契约：双 `npx tsc -p tsconfig.json --noEmit` + `npx tsc -p tsconfig.main.json --noEmit` 0 错（classifyAction 移除后无悬挂引用——renderer 6 处 + main isReadOnlyBash 全切换）
- [ ] L3 交互：`npx playwright test --project=interaction`——**相关场景重写后全量回归 ≥42** + S6 新场景（curl localhost 自动放行/外网 curl ask 弹卡——拍板 3 执行流落地）
- [ ] 冒烟：`node e2e-suite.mjs` 或等价冒烟场景（S6 门禁新增——授权执行流端到端：只读自动/清单内自动/越界 ask/高危 deny 四路径）——L3 承载即满足
- [ ] Lint 门禁：`npx eslint .` 0 errors；`npm run format:check` 全过
- [ ] 行为验收：
  - [ ] **main preApproval 改引用 classifyReadonly**（§8.1 A ①）：isReadOnlyBash 从 classifyAction 薄包装改为 classifyReadonly 直连——bash readonly → auto 放行；**network-read localhost → auto 放行（拍板 3——main 侧同步）**；network-read 外网 → ask（弹授权卡——安全默认）；hazardous → ask
  - [ ] **renderer side-effect 判定统一领域层同源**（坑 97——6 处 classifyAction === 'side-effect' 改调领域层 isSideEffectAction）：readonly/network-read localhost → 非 side-effect（自动放行不弹卡）；in-plan/out-of-plan/hazardous/network-read 外网 → side-effect（构成执行确认点/拦截）
  - [ ] **拍板 3 全链落地**：curl localhost GET/HEAD 自动放行（renderer 不弹卡 + main preApproval 放行）；外网 curl GET → ask（授权卡）——L3 场景断言
  - [ ] **classifyAction 兼容壳移除**（S6 由 classifyReadonly/actionGate/isSideEffectAction 直连取代——renderer/main 零消费）
  - [ ] **actionGate 策略接线确认**（不变量 3 全量）：sessionGate 优先 → actionGate——readonly allow / network-read（localhost allow·外网 ask）/ in-plan allow / out-of-plan ask / hazardous ask（policy 可 deny）——canExecute deny 拦截 + ask 走授权卡闭环（既有）
- [ ] 审计状态：本阶段 open 项（如有）fixed/recorded；覆盖矩阵已更新
- [ ] 决策日志同步：本阶段裁定 → docs/decisions/ 有 ADR（如有）
- [ ] 已 push + CI 绿（qa.yml run）

## TDD 网格（spec-first + test-first）

| 功能 | 规范断言（先写——来源） | 失败测试（红） | 实现（绿） | 重构 |
|------|------------------------|---------------|-----------|------|
| isSideEffectAction 领域层同源判定 | §3.3 actionGate 语义 + 拍板 3（localhost 自动/外网 ask——315 行）+ 坑 97（渲染层与异步链同源） | tests/unit/conversationState.test.ts（readonly 非 side-effect；network-read localhost 非；network-read 外网是；write/edit 是；bash hazardous 是；空命令是——fail-closed） | conversationState.isSideEffectAction（classifyReadonly + isLocalhostCommand 同源——与 actionGate localhost 判定共享） | classifyAction 兼容壳移除（S6 直连取代——renderer 6 处 + main 1 处切换） |
| main preApproval 改引用 classifyReadonly | §8.1 A ①（main 必须同步引用新判定——否则 renderer/main 分裂重现）+ 拍板 3（main preApproval 同步放行 localhost） | tests/unit/conversationState.test.ts 或 tools 相关（isReadOnlyBash 语义扩展——如 main 侧函数测试：readonly auto / network-read localhost auto / network-read 外网非 auto / hazardous 非 auto） | tools.ts isReadOnlyBash 改调 classifyReadonly + isLocalhostCommand（network-read localhost → auto） | classifyAction import 删除 |
| 拍板 3 全链（curl localhost 自动/外网 ask） | 315 行拍板 3（curl 对 localhost 自动放行，外网 GET ask——安全默认） | tests/interaction/cards-from-decision-content.interaction.ts 或 core（S6 场景：模型调 bash curl localhost GET → 自动 done 无授权卡；外网 curl GET → need-approval 授权卡） | renderer side-effect 切换（isSideEffectAction——localhost 不算）+ main preApproval 放行——全链 | — |
| classifyAction 移除 | §8.1 A（classifyReadonly 直连取代——兼容壳 S6 移除） | L2 双 tsc（移除后无悬挂引用——renderer/main 全切换） | conversationState.classifyAction 删除 | — |

## 产出物

- src/domain/conversationState.ts：isSideEffectAction（领域层同源）+ isLocalhostCommand（actionGate 与 isSideEffectAction 共享——localhost 判定单源）+ classifyAction 删除
- src/main/tools.ts：isReadOnlyBash 改调 classifyReadonly + localhost 放行（preApproval 语义升级——network-read localhost 自动）
- src/renderer/ConversationPanel.tsx：6 处 classifyAction === 'side-effect' 改调 isSideEffectAction + import 清理
- tests/unit/conversationState.test.ts 扩展（isSideEffectAction/isLocalhostCommand + classifyAction 移除后回归）
- tests/interaction/（S6 新场景 ≥2：curl localhost 自动放行/外网 ask 弹卡）
- docs/tests/coverage-matrix.md 更新（S6 用例 + 门控双维接线）
- 冒烟门禁：L3 场景承载（四路径：只读自动/清单内自动/越界 ask/高危 deny——既有场景覆盖 + S6 新增）

## 边界（不做——防蔓延）

- 授权疲劳/信任领域（authModel/taskTrust/delegateLowRisk）不动（V2——设计 369 行：actionGate risk 分级**复用**现有 toolRisk/canMergeApprove——不重构）
- 拒绝记忆（deniedApprovals——§3.4 C6「同轮同类动作直接 deny」+ gate.denied 事件）不动（S7 或 V2——本阶段只接线 ask/deny 既有判定）
- 风险分级 UI（授权卡风险明示/合并授权）不动（既有 toolRisk/canMergeApprove 保持）
- L4/L5 不跑（S7 全链——S6 冒烟由 L3 场景承载）
- ADR-001 §3.4 C2（pending 自由文本=隐式 reject）保持观察
- gate.denied 事件登记（§3.5）不动（拒绝记忆机制落定时——见边界 2）
