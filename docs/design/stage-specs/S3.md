# Stage S3 Spec（renderer 接线）

> 来源：docs/design/intent-confirmation-domain-design.md §6 S3 + §8.2 E（会话持久化）+ §8.3 F（坑 101 根治）+ §3.4（拒绝带原因）+ §4.1（拒绝超限回退）。
> 状态：定稿（2026-08-16 开工）。
> 前置：S1 领域层（decisionContent 快照/userDecided/approvalDecided/deriveDecisionPoint）+ S2 解析器（parsePlanProposal/parseCompletionClaim）+ sysPrompt ⑬⑭⑮ 契约（已就位）。S3 = 消费侧接线——卡渲染从 decisionContent 派生，不再从模型文本标记直接判定。

## DoD（机器可验证断言——stage-gate 逐条执行）

- [ ] L1 全量绿：`cd apps/desktop && npx vitest run`（新增用例 ≥10 条——见 TDD 网格）
- [ ] L2 契约：双 `npx tsc -p tsconfig.json --noEmit` + `npx tsc -p tsconfig.main.json --noEmit` 0 错
- [ ] L3 交互：`npx playwright test --project=interaction`——**相关场景重写后全量回归 ≥31**（旧基建场景保留 + S3 新场景：方案卡/拒绝原因/触发权切换）
- [ ] Lint 门禁：`npx eslint .` 0 errors；`npm run format:check` 全过
- [ ] 行为验收：
  - [ ] **触发权切换**：卡渲染条件从「模型文本标记（hasPlan/achievedMatch/goalFallback 正则）」改为「领域状态派生（deriveDecisionPoint + decisionContent）」——pending 非 none 时卡渲染唯一来源 = stateRef.current.decisionContent（子断言：goal/plan/resolution/approval 四类卡均从 decisionContent 取内容；无 decisionContent 不弹卡）
  - [ ] **方案卡渲染**：plan 卡展示 PlanProposal 三要素（文件清单含原因/关键假设/验证计划）——L3 场景断言 DOM 内容
  - [ ] **拒绝原因 UI**：拒绝按钮收集 RejectReason（kind + 可选 text/target）→ userDecided(confirm:false, reason)（不变量 8——无原因不产生拒绝转换）——拒绝后卡隐藏（坑 101 根治：拒绝处理标记与状态回退同源，不再依赖 rejectedCardIdx 异步时序）
  - [ ] **会话持久化 decisionContent**：serializeMessages/loadSession 支持 decisionContent（PlanProposal/CompletionClaim 结构）——恢复后决策点内容不丢（L1 序列化往返用例）
  - [ ] **清单匹配统一**（审计遗留 S3 项）：renderer inPlannedFiles（trustPath 归一）改为引用领域层 sessionGate 同源判定（Q5——单一权威；传参含 rootPath 归一化基准）
  - [ ] **拒绝超限回退**（§4.1）：rejectStreak ≥3 → 状态栏提示「连续拒绝——建议澄清需求或重新描述」（不弹卡轰炸；L3 场景）
  - [ ] **A-003 关闭**：proposal.* 事件 emit 断言（parse 成功/失败 → proposal.plan 带 ok/reason；接线后 timelineEvents.test.ts 补）
- [ ] 审计状态：A-003 fixed（本阶段项）；其余 open 项（如有）fixed/recorded
- [ ] 覆盖矩阵已更新（S3 新增用例 + 事件断言）
- [ ] 决策日志同步：本阶段裁定 → docs/decisions/ 有 ADR
- [ ] 已 push + CI 绿（qa.yml run）

## TDD 网格（spec-first + test-first）

| 功能 | 规范断言（先写——来源） | 失败测试（红） | 实现（绿） | 重构 |
|------|------------------------|---------------|-----------|------|
| 触发权切换（卡从 decisionContent 派生） | §3.3 deriveDecisionPoint 语义 + §8.2 E：渲染唯一来源 = decisionContent（pending 时）；模型文本不再直接判定卡内容 | tests/interaction/cards-from-decision-content.interaction.ts（场景：目标提议→goal 卡内容=proposal.statement；方案提议→plan 卡含 files/assumptions/verificationPlan；完成声明→resolution 卡；无 decisionContent 不弹） | ConversationPanel 卡渲染改读 stateRef.current.decisionContent（含 approval） | 删除 hasPlan/achievedMatch/goalMatch 文本判定路径（pendingCardToShow 兼容壳移除或降级）；坑 101 拒绝时序根治（拒绝处理同步标记） |
| 方案卡 UI | §3.2 PlanProposal：文件清单（path+reason）+ 假设 + 验证计划 | 同上 interaction（DOM 断言三要素可见） | nf-confirmcard 方案卡子组件（列表渲染） | — |
| 拒绝原因收集 | §3.4 userDecided(confirm:false, reason) 不变量 8：拒绝必带原因 | tests/unit/useConversationState.test.ts（reject 带 kind/text → 状态回退 + rejectStreak+1；无 reason 转换被拦） | useConversationState.reject 扩展（reason 参数）+ 拒绝按钮收集（kind 预设 + 可选 text） | userRejected 兼容壳语义对齐（S3 移除声明） |
| 会话持久化 decisionContent | §8.2 E：serialize/load 往返不丢（含 PlanProposal/CompletionClaim 结构）；恢复后 pending 冻结立即生效 | tests/unit/sessionStore.test.ts（往返用例：goal/plan/resolution 决策点快照；loadSession 后 decisionContent 完整） | sessionStore serialize/load 扩展 decisionContent 字段 | 恢复时序：load 后 setPending 冻结（C5） |
| 清单匹配统一 | 审计 Q5：sessionGate.inPlannedFiles 权威，renderer 引用（trustPath 归一传参） | L1 conversationState.test.ts::sessionGate 清单匹配（相对/绝对/中文名）+ L3 写文件场景 | renderer inPlannedFiles 改调用领域层（或导出同源判定函数） | renderer 本地 trustPath 循环删除（双源消除） |
| 拒绝超限回退 | §4.1：rejectStreak ≥3 提示澄清（不循环弹卡） | L3 interaction（拒绝 3 次 → 状态栏提示出现，卡不再弹） | 渲染层消费 rejectStreak（状态栏提示 + 卡抑制） | — |
| proposal.* 事件断言 | §8.2 D：parse 结果 → proposal.plan（ok/reason 载荷） | tests/unit/timelineEvents.test.ts::proposal.*（emit 接线后断言） | 接线处 emit（ConversationPanel done 分支 parse 后） | A-003 关闭 |

## 产出物

- ConversationPanel.tsx：卡渲染触发权切换 + 方案卡/拒绝原因 UI + rejectStreak 消费 + proposal.* emit
- useConversationState.ts：reject 扩展（reason）/ 恢复时 pending 冻结
- sessionStore.ts：decisionContent 序列化
- src/domain/conversationState.ts：inPlannedFiles 导出（renderer 引用）或同源判定函数
- tests/interaction/cards-from-decision-content.interaction.ts（新场景）
- tests/unit/{sessionStore,useConversationState,timelineEvents}.test.ts 扩展
- docs/tests/coverage-matrix.md 更新
- A-003 关闭（fixed + 证据）

## 边界（不做——防蔓延）

- 不接完成证据对账 UI（verifyCompletion 消费 + evidence_missing 打点 = S4——resolution 卡确认条件仍为旧 producedFiles 检查）
- 不替换 forceTool（decideProgressGuarantee = S5）
- main preApproval/actionGate 不动（S6）
- L4/L5 不跑（S7 全链）
- 持久化的 pending 冻结只做加载语义（模型首轮约束 S3 内——sysPrompt 已含 ⑧ 纯文本格式，恢复时序由 UI 门控）
