# Stage S2 Spec（提议解析）

> 来源：docs/design/intent-confirmation-domain-design.md §6 S2 + §3.3（parsePlanProposal/parseCompletionClaim/verifyCompletion）+ §8.1 C（sysPrompt ⑬⑭⑮ 契约同步）。
> 状态：初稿（2026-08-16——开工前定稿）。

## DoD（机器可验证断言——stage-gate 逐条执行）

- [ ] L1 全量绿：`npx vitest run`（新增值对象解析用例 ≥15 条——见 TDD 网格）
- [ ] L2 契约：`npx tsc -p tsconfig.json --noEmit` + `npx tsc -p tsconfig.main.json --noEmit` 0 错
- [ ] L3 交互：`npx playwright test --project=interaction` 31/31（旧行为不回归——S3 前旧基建仍用）
- [ ] 行为验收：
  - [ ] `parsePlanProposal` 对设计 §8.1 C 格式的【执行方案】块解析出 PlanProposal（files 含理由/assumptions/verificationPlan）
  - [ ] 解析失败（格式漂移）→ `{ ok: false, reason: 'no-block' | 'malformed' }`——不产生决策点（deriveDecisionPoint 无 plan 提议）
  - [ ] 路径合法性过滤继承（坑 102：垃圾条目不进清单——derivePlannedFiles 输入干净）
  - [ ] `parseCompletionClaim` 解析【已达成】块 → CompletionClaim（summary + evidence 三字段）
  - [ ] `verifyCompletion` 扩展 systemState 系统代跑核验（V1a：只读验证命令重跑核对输出；V1b：diffs 系统派生）——unverifiable 语义保持（S1.1 单源）
  - [ ] sysPrompt ⑬⑭⑮ 与解析器格式契约互锁（解析器契约测试覆盖提示词要求的输出格式）
- [ ] 审计状态：S1.1 遗留 open 项全部 fixed / recorded（外网 curl 过渡语义锁定已 fixed——核对）
- [ ] 覆盖矩阵首版已产出（docs/tests/coverage-matrix.md——Inv 1-8 ↔ L1 测试 ↔ 事件）
- [ ] 决策日志同步：本阶段裁定（如有）→ docs/decisions/
- [ ] 已 push + CI 绿

## TDD 网格（spec-first + test-first——先写规范断言与失败测试，再实现）

| 功能 | 规范断言（先写） | 失败测试（红） | 实现（绿） | 重构 |
|------|-----------------|---------------|-----------|------|
| parsePlanProposal | §3.3：结构化解析【执行方案】块：文件清单（含原因）+ 假设行 + 验证计划行；路径合法性过滤保留 | tests/unit/planProposalParser.test.ts（契约用例：合法块/缺假设/缺验证计划/格式漂移/垃圾路径） | 实现解析器（纯函数） | 与 parseExecutionPlan 旧函数并存期语义对照 |
| parseCompletionClaim | §3.3：【已达成】块 → summary + evidence（verification/diffs/pendingQuestions） | tests/unit/completionClaimParser.test.ts | 实现解析器 | — |
| verifyCompletion V1a/V1b | §3.3：系统代跑只读验证命令核对输出；diffs 由 plannedFiles/producedFiles 派生比对 | tests/unit/verifyCompletionSystem.test.ts（mock systemState 代跑） | 扩展 verifyCompletion（systemState 参数） | 与 S1.1 evidenceVerifiable 单源保持一致 |
| sysPrompt 契约 | §8.1 C：⑬ 目标提议格式（含「关键假设」行）；⑭ 方案提议（文件清单含理由+假设+验证计划）；⑮ 完成声明（声明+验证证据+遗留问题）——编号以 sysPrompt.ts 现状为准 | sysPrompt.test.ts 契约用例（输出格式 ↔ 解析器输入互锁） | 更新 sysPrompt.ts | 与解析器同 commit（格式契约同步） |

## 产出物

- `src/domain/planProposalParser.ts`（或并入 conversationState.ts 旁新模块——按代码组织裁定）
- `src/domain/completionClaimParser.ts`
- `verifyCompletion` systemState 扩展（V1a/V1b）
- `sysPrompt.ts` ⑬⑭⑮ 更新（与解析器同 commit）
- `tests/unit/{planProposalParser,completionClaimParser,verifyCompletionSystem}.test.ts`
- `docs/tests/coverage-matrix.md`（首版）
- timeline 事件：`proposal.plan` / `proposal.completion` 登记（§8.2 D——domain 联合补 'proposal' 成员）

## 边界（不做——防蔓延）

- 不接 renderer（卡渲染/decisionContent 序列化 = S3）
- 不做 S4 证据对账的 UI 引导（completion.evidence_missing 打点 = S4）
- 不替换 forceTool（decideProgressGuarantee 接线 = S5）
- main preApproval 不动（actionGate 接线 = S6）
- L4/L5 不跑（S7 全链）
