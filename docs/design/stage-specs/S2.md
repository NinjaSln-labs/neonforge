# Stage S2 Spec（提议解析）

> 来源：docs/design/intent-confirmation-domain-design.md §6 S2 + §3.3（parsePlanProposal/parseCompletionClaim/verifyCompletion）+ §8.1 C（sysPrompt ⑬⑭⑮ 契约同步）+ §8.2 D（proposal.* 事件登记）。
> 状态：定稿（2026-08-16 开工）。
> 前置：S1 已落地值对象（GoalProposal/PlanProposal/CompletionClaim/CompletionEvidence/VerificationItem——conversationState.ts §3.2）与 verifyCompletion 纯逻辑版；本阶段新增解析器 + systemState 扩展 + 提示词契约同步 + 事件登记。

## DoD（机器可验证断言——stage-gate 逐条执行）

- [ ] L1 全量绿：`cd apps/desktop && npx vitest run`——新增用例 ≥20 条（TDD 网格 3 个解析器文件 + sysPrompt 契约 + verifyCompletion 扩展；**oracle 需领域核对防假绿**——每个解析用例的期望值对照设计 §3.3/§8.1 C 原文）
- [ ] L2 契约：`npx tsc -p tsconfig.json --noEmit` + `npx tsc -p tsconfig.main.json --noEmit` 0 错
- [ ] L3 交互：`npx playwright test --project=interaction` 31/31（旧基建不动——S3 才迁移；解析层无接线，行为不回归）
- [ ] Lint 门禁：`npx eslint .` 0 errors（6 既有 exhaustive-deps warnings 可留）；`npm run format:check` 全过
- [ ] 行为验收：
  - [ ] `parsePlanProposal` 对 §8.1 C ⑭ 格式【执行方案】块解析出 PlanProposal（summary + files[{path,reason}] + assumptions + verificationPlan）——契约用例：合法块 / 缺假设行 / 缺验证计划 / 格式漂移（no-block vs malformed 归因）/ 垃圾路径过滤（坑 102 继承：无空白=合法、含空白须带扩展名）
  - [ ] 解析失败 `{ ok: false, reason: 'no-block' | 'malformed' }`——no-block = 无【执行方案】标记；malformed = 有标记但块内无合法文件行
  - [ ] `parseCompletionClaim` 对 §8.1 C ⑮ 格式【已达成】块解析出 CompletionClaim（summary + evidence.verification[command/output/passed] + evidence.diffs + evidence.pendingQuestions）——契约用例：完整块 / 缺证据 / 缺遗留问题 / 无标记（返回 null）/ 格式漂移容错
  - [ ] `verifyCompletion` 扩展：签名含 `systemState`（V1a 系统代跑只读验证命令核对输出——mock 系统执行器返回成功/失败/非只读；V1b diffs 由 plannedFiles/producedFiles 系统派生比对）；unverifiable 语义保持（S1.1 单源——evidenceVerifiable/isSystemVerifiable 复用）；非只读验证命令 → unverifiable 计入返回
  - [ ] sysPrompt ⑬⑭⑮ 与解析器格式契约互锁：⑭ 含「文件清单（原因）+ 假设 + 验证计划」行格式；⑮ 含「验证证据 + 遗留问题」结构——**sysPrompt.test.ts 契约用例覆盖提示词输出格式 ↔ 解析器输入**（同 commit 同步，防漂移）
  - [ ] timeline 事件：`proposal.plan` / `proposal.completion` 登记（TIMELINE_EVENT_SPECS + 06 文档同步——§8.2 D；parse-error 时 detail 带 parse-error: reason）
- [ ] 审计状态：S1.1 遗留 open 项核对——**本阶段应处理项**（verifyCompletion V1a/V1b、proposal.* 事件）fixed；**属后续阶段项**（外网 curl ask 消费=S6、清单匹配统一=S3、拒绝超限回退=S3、completion.evidence_missing=S4、gate.denied=S6）recorded（理由=阶段归属，入 audit-items 或注记）
- [ ] 覆盖矩阵首版已产出（`docs/tests/coverage-matrix.md`——coverage-matrix skill：Inv 1-8 ↔ L1 测试 ↔ TIMELINE_EVENT_SPECS ↔ DoD 断言三向矩阵，缺口入 audit-items）
- [ ] 决策日志同步：本阶段裁定（如有）→ docs/decisions/（阶段裁定写 ADR 只引用编号）
- [ ] 已 push + CI 绿（qa.yml run success）

## TDD 网格（spec-first + test-first——先写规范断言与失败测试，再实现）

| 功能 | 规范断言（先写——来源） | 失败测试（红） | 实现（绿） | 重构 |
|------|------------------------|---------------|-----------|------|
| parsePlanProposal | §3.3 + §8.1 C ⑭：结构化解析【执行方案】块 → PlanProposal（summary/files 含 reason/assumptions/verificationPlan）；坑 102 路径过滤继承；失败降级 ok:false（no-block/malformed） | tests/unit/planProposalParser.test.ts（契约用例：合法块全字段 / 缺假设行 / 缺验证计划 / 无标记→no-block / 有标记无文件行→malformed / 垃圾路径行（备注句/纯说明）过滤 / 中文文件名含空格带扩展名） | src/domain/planProposalParser.ts（纯函数） | 与 parseExecutionPlan 旧函数并存期语义对照（旧函数 S3 移除——renderer 未接线前保留）；路径过滤逻辑抽共享（isLikelyPath 复用/迁移） |
| parseCompletionClaim | §3.3 + §8.1 C ⑮：结构化解析【已达成】块 → CompletionClaim（summary + evidence 三字段）；无标记 → null | tests/unit/completionClaimParser.test.ts（完整块 / 缺 verification / 缺 pendingQuestions / 无标记→null / 半结构化容错——命令行与说明混排） | src/domain/completionClaimParser.ts（纯函数） | — |
| verifyCompletion systemState 扩展 | §3.3（V1a/V1b）：systemState 代跑只读验证命令核对输出；diffs 系统派生；unverifiable 保持单源 | tests/unit/verifyCompletionSystem.test.ts（mock 执行器：只读命令代跑成功→ok / 代跑输出不符→missing / 非只读→unverifiable / pendingQuestions→missing / diffs 派生比对一致/缺失） | conversationState.ts verifyCompletion 扩展（systemState 参数——S1 纯逻辑版签名向后兼容或按 §3.3 签名更新） | evidenceVerifiable/isSystemVerifiable 复用（不变量 4 单源不破） |
| sysPrompt ⑬⑭⑮ 契约 | §8.1 C：⑭ 方案格式（文件清单原因 + 假设 + 验证计划）；⑮ 完成声明格式（声明 + 验证证据 + 遗留问题）；⑬ 目标提议含「关键假设」行 | tests/unit/sysPrompt.test.ts（契约用例：提示词文本包含解析器所要求的行格式锚点——⑬ 假设行 / ⑭ 假设+验证计划 / ⑮ 证据+遗留问题；编号以 sysPrompt.ts 现状为准不锁定） | src/renderer/sysPrompt.ts ⑬⑭⑮ 更新 | 与解析器同 commit（格式契约互锁——防「提示词改了解析器没跟」漂移） |

## 产出物

- `src/domain/planProposalParser.ts`（parsePlanProposal + 路径过滤共享）
- `src/domain/completionClaimParser.ts`（parseCompletionClaim）
- `conversationState.ts`：verifyCompletion systemState 扩展（V1a 代跑 + V1b diff 派生）
- `src/renderer/sysPrompt.ts`：⑬⑭⑮ 契约同步（与解析器同 commit）
- `src/domain/timeline.ts`：proposal.plan / proposal.completion 事件登记（+ docs/domain/06-domain-events.md 同步）
- `tests/unit/{planProposalParser,completionClaimParser,verifyCompletionSystem,sysPrompt}.test.ts`
- `docs/tests/coverage-matrix.md`（首版——coverage-matrix skill）
- audit-items 核对（S1.1 遗留项状态化：本阶段项 fixed / 后续阶段项 recorded）

## 边界（不做——防蔓延）

- 不接 renderer（卡渲染从 decisionContent 派生/方案卡 UI/拒绝原因 UI = S3；parseExecutionPlan 旧函数保留至 S3）
- 不做 S4 证据对账 UI 引导（completion.evidence_missing 打点 = S4——本阶段只登记 proposal.* 事件）
- 不替换 forceTool（decideProgressGuarantee 接线 = S5）
- main preApproval 不动（actionGate 接线 = S6；外网 curl ask 消费 = S6）
- 拒绝超限回退（rejectStreak ≥3 消费 = S3）
- 会话持久化 decisionContent 序列化 = S3（§8.2 E）
- L4/L5 不跑（S7 全链）
