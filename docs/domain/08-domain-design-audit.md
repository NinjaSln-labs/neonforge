# NeonForge 领域设计审计（无阶段·目标驱动版 v2.0）

> 日期：2026-08-07
> 范围：`docs/domain/` 无阶段版（00 v3.0 / 02 / 03 / 04 / 05 / 06）
> 对照：无阶段目标驱动领域模型（确认驱动推进——目标/执行/达成三确认点）
> 方法：领域设计完整性审计（确认点/推进门控/执行保障/宿主边界/能力环境/结构化确认/时间线）

---

## Verdict

**Critical：0。Major：2。Minor：3。**

无阶段领域模型（00 v3.0）已自洽：确认点=推进门槛（结构性）、执行保障四规则、宿主强制边界、能力=环境视图（单源）、结构化确认（行业共识）——设计完整。

**设计就绪度：90 / 100**

---

## 分维评分

| 维度 | 分 | 说明 |
|------|----|------|
| 确认点完整性 | 95 | 目标/执行/达成三确认点 + 三态（等待/确认/拒绝）——覆盖任务全生命周期 |
| 推进门控 | 90 | 确认点=推进门槛（结构性——状态机未到下一态）——未确认目标不执行/未确认执行不动手/未确认解决不收敛 |
| 执行保障 | 90 | forceTool 四规则（确认后无产出强制/失败释放/计划写完释放/达成确认释放）|
| 宿主强制边界 | 90 | 计划清单（追加语义/显式可见/拒绝带边界）——行业共识（Claude Code/Codex/Aider）|
| 能力环境 | 92 | 环境=事实来源（一次检测）/ 能力=语义视图（推导）——消除双源（调研定论）|
| 结构化确认 | 92 | 确认卡（确认/拒绝）——行业共识（OpenHands/Cline/Codex）——非确认词匹配 |
| 时间线 | 88 | 全步骤统一记录（用户/搭档/工具/授权/确认/状态）——可观测性 |

---

## 审计项

### 1. 确认点完整性（通过）

| 确认点 | 设计 | 状态 |
|--------|------|------|
| GoalConfirmed | 用户确认目标（确认/重新描述）——未确认只澄清 | ✓ |
| ExecutionConfirmed | 用户确认执行（确认/修改方案）——未确认不动手 | ✓ |
| AchievementConfirmed | 用户确认解决（已解决/还要改）——未确认不收敛 | ✓ |

**Major 1**：执行确认的「活动边界」——未确认时模型可给方案（read/search）但不可 write/edit/bash——边界动作清单应细化到工具级（当前设计「不产生执行动作」——需明确 read/check-capability 放行清单）。

### 2. 推进门控（通过）

确认点未确认 → 模型停在该确认点（结构性——非拦截）。状态机：clarifying → goal-confirmed → executing → achieved-reported → resolved。

### 3. 执行保障（通过）

| 规则 | 设计 |
|------|------|
| 确认后无产出 | forceTool=true（required——防只说不做）|
| 工具失败 | 释放（auto——模型诊断修正——required 压制诊断是反模式）|
| 计划写完 | 释放（模型可收敛——写 1 文件 ≠ 任务达成）|
| 达成确认 | 释放（用户确认解决——对话收敛）|

### 4. 宿主强制边界（通过）

计划清单（追加语义/显式可见/拒绝带边界）——模型漂移防护（行业共识）。

**Major 2**：计划清单与执行确认的关系未显式定义——「执行确认卡未点」与「文件不在批准清单」是两个边界（执行级 vs 文件级）——门控优先级需明确（执行未确认 → 全部执行动作挂起；执行已确认 → 再按清单判定）。

### 5. 能力环境（通过）

环境=事实来源（一次检测）→ 能力=语义视图（推导）——Ledger 回填（自学习）——环境快照注入模型。

### 6. 结构化确认（通过）

确认卡（确认/拒绝按钮）——对齐 OpenHands USER_CONFIRMED/USER_REJECTED、Cline allow/always/reject。

**Minor 1**：拒绝动作的后续语义——「重新描述/修改方案/还要改」是拒绝后的引导——应标准化（拒绝 → 模型收到拒绝事件 → 重试/调整）。

### 7. 时间线（通过）

全步骤统一记录（JSONL）——崩溃保留已写行。

**Minor 2**：时间线事件类型与确认点事件（goal/execution/achievement confirmed+rejected）应完整（当前已列）。

**Minor 3**：多会话隔离（session 字段）——跨进程 seq 冲突（进程内自增）——需全局唯一（ts+进程 id）。

---

## 与旧版审计（v1.0——六阶段）的关系

旧 v1.0 审计（8 Critical + 14 Major——六阶段领域不收敛）已被无阶段重构消化：旧 Critical（模型策略/BC 清单/AgentChain 规格化/EventBus/Preheating 归属）全部随六阶段范式废弃——无阶段后这些概念（AgentChain 流水线/Compaction/PrefixCache/ContextEngine）不再构成领域核心。

---

## 结论

无阶段目标驱动领域模型设计完整（90/100）——2 个 Major（执行边界工具级清单、门控优先级）在实现对齐时闭合；3 个 Minor 随实现细化。
