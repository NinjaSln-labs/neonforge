# NeonForge 产品设计规范

> version: 2.1
> 目标：一份完整的产品设计，能直接指导工程实现。
> 修订 2.1（2026-08-15）：无阶段化确认收尾——确认卡（目标/执行/达成）+ 会话级单一 PENDING + approve-files 已入正文。
> 修订 2.2（2026-08-16，待 UI 落地精化）：意图确认交互升级（`intent-confirmation-domain-design.md`）——方案确认卡升级为**可审阅方案卡**（文件清单+模型假设+验证计划——批准/修改/重出）；达成确认升级为**解决确认（证据对账）**——模型完成声明必须附可核验证据（验证命令输出/测试结果/diff），证据不足不弹确认卡、引导补证据；拒绝操作带**原因选项**（方向/范围/复杂度/缺信息）回填模型调整。
> 修订 2.0（2026-08-02）：定位重定义——「帮用户解决当前的问题」AI 问题工作台（非 IDE）。依据 `.scratch/neonforge-v1/project-alignment.md`（用户裁决终版 + 多模型补充）。核心变化：品类（问题工作台）、用户流（说问题→授权→交付→反馈）、信任阶梯、问题台账/交付包。
> 修订 1.1：交叉审计修复（历史）。

---

## 一、产品定义

### 1.1 这是什么

NeonForge 是为 DeepSeek 打造的 **AI 问题工作台**：用户用自然对话说出"当前的问题"，NeonForge 通过内在的工程/设计/编排自动推进，交付解决结果。

**用户流**：`说出问题 → 分析 → 目标/方案确认（方案卡——文件+假设+验证计划）→ 分步授权执行 → 解决确认（证据对账——交付≠解决）→ 交付结果（数字产物/修复闭环）→ 反馈 + 指导继续`

- **能力边界**：一切能被数字工具解决的直接解决；超出（域名/备案/线下履约）→ 交付数字产物 + 分步指导，**不装能**
- **不是**：IDE（编辑器为中心）、Chatbot（只聊不交付）、多模型客户端

### 1.2 核心原则（所有设计决策的源头）

1. **帮用户解决当前的问题。** 一切围绕"问题被解决"（问题关闭），不是"我给你写了代码"。
2. **分步授权，每一步可控。** 分析（只读）自动；写入/执行/部署逐级授权（信任阶梯 L1-L4）；可随时撤销。
3. **交付可验证。** DoD 前置（动手前对齐"什么叫解决"+ 验收标准）（DoD 对齐 ⏸ V2——2026-08-15 裁决；V1 以解决确认卡（证据对账）+交付包为可验证闭环）、证据链（做了什么/怎么验）、失败可恢复（失败包/备份/回滚/半成品可带走）。
4. **不装能。** 超出数字能力 → 明说 + 给产物 + 分步指导；不静默失败、不假装搞定。
5. **上手不学。** 零学习成本：说出问题就会被解决；不学指令/不学 IDE/不注册。
6. **安静可信。** 不打扰、不炫技；过程可见但不说术语；本地优先、数据出域显式。

**原则 → 设计映射表**

| 原则 | 落地设计（§对应） | 反例（违规即缺陷） |
|------|------------------|-------------------|
| 1 解决问题 | 问题台账/交付包（§3.3）、DoD 前置验收（§4.1）、北极星=问题关闭率+二次委托率 | 交付"我写了代码"而非"问题已解决可验证"；交付≠关闭 |
| 2 分步授权 | 信任阶梯（§4.2）、写入模型（§3.5 接受=暂存/写入=原子 apply）、可撤销 | 未授权写盘/部署；授权疲劳（步步弹窗）；无撤销 |
| 3 可验证 | DoD 对齐（§4.1——DoD 对齐 ⏸ V2——2026-08-15 裁决；V1 以解决确认卡（证据对账）+交付包为可验证闭环）、证据链（diff/日志/前后对照）、失败包（§5） | 只报"已完成"无证据；失败不说明影响/下一步 |
| 4 不装能 | 边界承诺（§1.3）、外部世界协调（§4.4：等待清单+触发条件+指导） | 假装搞定域名/备案/线下；硬做做不了的事 |
| 5 上手不学 | 场景卡片（§3.2）、非技术视图（§3.8）、人话交付 | 首次强制教程；核心依赖快捷键/术语 |
| 6 安静可信 | 非模态提示条、数据出域清单（§10）、本地优先 | 弹窗打扰；隐式上传；面向用户抛技术术语 |

**原则间冲突裁决**：
| 冲突 | 裁决 |
|------|------|
| 「2 分步授权」vs「5 上手不学」（非技术想一键 vs 每步确认） | 信任阶梯 + 非技术视图主路径「接受并写入」（改动说明先行，可一键）；高风险操作（删/部署）永远单独确认 |
| 「3 可验证」vs「6 安静」（证据 vs 不打扰） | 证据折叠在交付包内（人话摘要 + 详情展开），非模态 |

### 1.3 能力边界与首次使用

**能力边界（不装能）**：
- 数字工具能力内：直接解决（文件整理/数据加工/建网站源码/修系统/做小工具/0-1 交付）
- 超出数字能力（域名/备案/线下/法律）：交付数字产物 + 分步指导——明说"我做到哪、你必须做哪"

**首次使用前提**：需要 DeepSeek API Key。首次打开：
- 本地已有有效 Key：自动检测，直接进入启动页
- 没有：简洁配置页（粘贴 Key → 验证 → 进入）
- 网络不通：提示 + [跳过（离线不可用）]

零学习成本指不学 IDE 操作，不是零依赖。

### 1.4 用户

两类用户，一套内核、双视图（同源状态，可切换）：

- **不会写代码的人（V1 主）** — 说出问题 → 被解决：交付数字产物（文件/源码/小工具）+ 人话说明 + 指导。看「非技术视图」（产物/预览/验收清单），不暴露术语。
- **会写代码的开发者** — 授权闭环：异常修复（定位→方案→修复→测试→部署→复核→反馈）/ 0-1 交付（软件工程模型/敏捷）。可展开技术细节、可手动编辑（编辑器=可选能力）。

---

## 二、信息架构

```
NeonForge
│
├── 首次配置页（无有效 API Key 时出现）
│   └── 粘贴 Key → 验证 → 启动页
│
├── 启动页
│   ├── 打开已有项目
│   └── 从零开始
│
├── 主工作区（打开项目后 · AI Agent IDE 三区）
│   ├── 会话区（左）：会话列表 / 新建会话
│   ├── 对话区（中，核心最大）：消息流 + 输入框 + 任务 Tab（思考中消息背景呼吸）
│   └── 产物区（右）：工程（文件树）/ 产物（文件只读查看/diff）
│   └── 状态栏：agent 状态（就绪 / 思考中 / 待批准）
│
├── 新建项目流
│   ├── 输入描述
│   ├── 方案确认
│   └── 逐步搭建 + 审核
│
└── 设置（最小）
    └── 语言 / 默认视图 / 主动提醒开关
```

---

## 三、页面规格

---

### 3.1 首次配置页

**触发条件：** 应用首次打开且检测不到有效的 DeepSeek API Key。

**布局：**

```
┌──────────────────────────────────────────┐
│                                          │
│           NeonForge                       │
│                                          │
│        需要 DeepSeek API Key              │
│                                          │
│    ┌──────────────────────────────┐      │
│    │  sk-xxxxxxxxxxxxxxxxxxxxxxxx  │      │
│    └──────────────────────────────┘      │
│                                          │
│    [验证并开始]                            │
│                                          │
│    ▸ 为什么需要？                          │
│      (折叠：NeonForge 通过 DeepSeek API   │
│       提供 AI 能力，Key 存储在你本地。)     │
│                                          │
└──────────────────────────────────────────┘
```

- 验证成功 → 进入启动页
- 验证失败 → 边框变红，提示"验证失败，请检查 Key"
- 网络不通 → 横幅提示，提供 [跳过（离线不可用）]

---

### 3.2 启动页

布局已整合到本章各节。修改以下：

**输入不完整的处理（修复审计 M4）：**

用户输入少于 5 个字且回车 → 不直接猜测。输入框下方出现两个选择芯片：

```
┌────────────────┐  ┌─────────────────────┐
│ 🆕 新建一个项目  │  │ 📂 打开文件夹改这个   │
└────────────────┘  └─────────────────────┘
```

用户点芯片选择路径，清晰无歧义。

**空状态例句及输入框交互：** 已整合到本章各节。

---

### 3.3 主工作区


**呼吸光条位置（修复审计 Minor 2）：**

在对话区/消息内（非独立面板顶部）。working 态靛蓝周期 2s，waiting 态琥珀周期 1s。

**状态栏：**

```
🟢 就绪 │ main │ 待审核: 4 处
```

- 显示：状态、分支名、改动状态（待审核 N 处 / 已暂存 N 处）
- 「接受」= 暂存改动（N=处改动），不立即写入
- 「全部接受并写入」= 原子 apply 到文件系统

**交付包底部（修复审计 M6）：**

折叠行，默认收起：
```
▸ 本轮用量
  （展开后：12K tokens │ 缓存命中 87%）
```

---

### 3.4 搭档的分析展示

**推理动效时间轴（以视觉规范 `product/05-visual-spec.md` §6.1 为权威）：**

```
t=0.0s   交付包/分析区滑入（0.25s），呼吸光条→靛蓝
t=0.3s   "🧠 怎么看这个问题的" 标题出现
t=0.5s   文件列表逐项出现（每项 0.1s）
t=1.3s   停顿
t=1.6s   "排除了这些可能" 逐项出现（每项 0.1s）
t=2.3s   停顿
t=2.6s   "确认"出现，结论文字蓝色
t=3.0s   推理展示完毕
t=3.2s   "📝 修改计划"出现
t=3.8s   分析展示完毕，呼吸光条→amber，按钮出现
```

用户在此期间点击其他地方或打字 → 剩余动画立刻全部显示。

---

### 3.5 Diff 审核

**默认审核模式（修复审计 M5）：**

用户点待审核任务或搭档改完后：

1. **先展示改动摘要**：哪些文件、几处改动、每处简述
2. 底部按钮：主按钮「全部接受并写入」、次按钮「逐处审查」
3. 用户点逐处审查 → 进入逐处模式（同原规格）

**写入时机（修复审计 M3）：**

- 逐处点 ✓ 接受 = 暂存（标记为已确认，不写文件）
- 点「全部接受并写入」或逐处审完点「写入文件」= 一次性原子 apply
- 状态栏显示「待审核: N」→ 接受后「已暂存: N」→ 写入后清零

**全部接受确认（修复审计 M1）：**

不用弹窗。diff 视图中出现确认提示条：

```
┌──────────────────────────────────────────────┐
│  确认写入全部 4 处改动？                        │
│  [写入] [取消]                                 │
└──────────────────────────────────────────────┘
```

提示条在编辑器底部，不遮挡，不模态。

**完成状态：**

```
✅ 改动已写入文件。
```

无 🎉，无庆祝动画。

---

### 3.6 任务列表

**多任务模型（修复审计 C5）：**

V1 采用**串行执行队列**：

- 可创建多个任务。同时仅一个在执行（分析/修改）
- 其他排队。当前任务审核完成后自动开始下一个
- 涉及同一文件：后续任务基于前一任务写入后的新版本执行
- 用户可拖动任务调整队列顺序

任务状态圆点重新设计（修复审计 Minor 10）：

| ● 蓝色实心 | 当前正在执行 |
| ◉ 蓝色半实心 | 待审核 |
| ○ 灰色空心 | 排队中 |
| ✓ 绿色 | 已完成 |
| ✗ 灰色 | 已取消 |

---

### 3.7 新建项目

**非技术闭环（修复审计 C2）：**

第四步完成态：

```
项目已创建。

[一键安装并预览]

（安装依赖 → 启动 → 应用内预览）
- 安装失败 → 展示错误 + [重试] [查看详情] [跳过]
- 端口占用 → [换个端口] [取消]
- 权限不足 → 提示文件夹权限
```

- 点击按钮后，自动执行 install + dev
- 进度显示在按钮位置（"正在安装依赖..." → "正在启动..."）
- 启动后在主工作区出现应用内预览面板
- 非技术用户不需要打开终端

---

### 3.8 非技术用户视图

审核模式（修复审计 M5）：

```
搭档改了你的博客首页：

改动说明:
· 标题字体变大了
· 文章卡片现在是两栏
· 加了"阅读更多"按钮

[全部接受并写入] [不满意，告诉搭档...]
```

点「不满意」展开输入框（批注等价）。默认展示摘要，接受为主路径。

### 3.9 上下文控制（修复审计 M2）

V1 提供最小上下文控制：

**@引用文件：**
- 搭档输入框输入 `@` → 弹出最近文件列表
- 选择 → 文件名以蓝色标签出现在输入框
- 搭档优先看被 @引用的文件

**搭档须知（项目规则）：**
- 项目根目录 `.neonforge` 文件
- 用户编辑自然语言：约定、技术栈、风格
- 搭档每次分析自动读取
- 文件树右键"创建搭档须知"可生成模板

---

## 四、交互模式

### 4.1 问题闭环流（v2 重定义）

```
说出问题（对话入口，零学习成本）
  → 分析（只读自动 · L1）→ DoD 对齐（"什么叫解决"+ 验收标准，用用户的话）（DoD 对齐 ⏸ V2——2026-08-15 裁决；V1 以解决确认卡（证据对账）+交付包为可验证闭环）
  → 方案确认（方案卡——文件清单+假设+验证计划，批准/修改/重出）→ 执行（L3 分步授权）
  → 交付包（产物 + 做了什么 + 验收对照 + 下一步/复跑入口）
  → 用户确认问题关闭（交付 ≠ 解决，以关闭为终态；解决确认=声明+证据对账——证据不足不弹卡）
  → 超出数字能力部分：产物 + 分步指导（不装能）
```

**确认卡（结构化确认——2026-08-07 无阶段定论；2026-08-16 触发权升级）**：需要用户决定的点（目标确认/方案确认/解决确认/工具授权）→ 对话内嵌**确认/拒绝卡片**——卡弹出 = **会话进入 PENDING（等用户决策）**——用户不决策则模型任何动作都无效（做了白做）——用户「是」→ 推进 /「否（带原因）」→ 调整。**触发权在系统**（决策点=状态×提议×动作属性的确定性派生——模型只能提议不能制造卡；方案卡=可审阅 PlanProposal，解决卡=证据对账）——对齐竞品：Claude Code/Gemini Plan 门、Cline Plan/Act。

- 每一步可暂停/撤销/回滚（失败包 + 备份 + 半成品可带走）
- 开发者场景（异常修复/0-1 交付）：同内核 + 部署授权（L4：备份→热更新→复核→反馈）

### 4.2 信任阶梯与状态变化表

**信任阶梯（L1-L4，渐进解锁，进度可见）：**

| 等级 | 能力 | 授权 | 典型 |
|------|------|------|------|
| L1 观察 | 只读分析/草稿/本地整理 | 首次全局授权，静默 | 分析问题、生成预览 |
| L2 建议 | 生成方案/补丁 | 建议呈现，一键应用 | 方案确认、PR |
| L3 操作 | 写文件/执行/网络 | 分步授权 + 快照 | 修改文件、跑命令 |
| L4 委托 | 高度信任重复任务 | 用户设规则，可降级撤销 | 格式化类自动应用 |

**问题状态机：**

| 状态 | 说明 | 用户下一步 |
|------|------|-----------|
| 理解中 | 分析问题、DoD 对齐（DoD 对齐 ⏸ V2——2026-08-15 裁决；V1 以解决确认卡（证据对账）+交付包为可验证闭环） | 补充/确认验收标准 |
| 方案待确认 | 方案卡（PlanProposal——文件清单+假设+验证计划）| [批准并执行] / [修改方案（带原因）] / [重出] |
| 执行中 | L3 分步授权推进 | 逐步授权或暂停 |
| 待你操作 | 需用户提供 X/确认 Y | 提供/确认 |
| 已交付 | 交付包就绪 | 验收 → 关闭或继续 |
| 已关闭 | 用户确认问题解决 | 下一问题或复跑 |
| 失败可恢复 | 失败包 + 回滚点 | 重试/换方案/人工接管 |

### 4.3 交付包（交付即闭环）

每个问题交付时产出**交付包**：
- 产物（文件/源码/修复摘要/数据）
- 做了什么（人话摘要）
- 验收对照（对 DoD 逐项打勾：怎么验/不对点哪里）
- 下一步 / 复跑入口（「上次那个再跑一遍」= 一等操作）
- 超出部分指导（数字能力外：分步指引）

### 4.4 外部世界协调（不装能落地）

- 明确建模「数字工具做不了但任务需要」的节点：域名/备案/第三方平台/线下履约
- 交付「等待清单 + 触发条件 + 你操作时的一键继续」，不假装已搞定

---

## 五、状态和错误处理

新增：

**场景 5：API Key 失效**

```
搭档对话中：
"API Key 好像失效了。要不要更新一下？"
[更新 Key]
```
点击后在对话区/产物区内嵌配置区，不跳页。

其他场景已整合到本章各节。骨架屏颜色见 `product/03-design-tokens.md`（深色 `#3E3E3E`）。

---

## 六、快捷键

| 快捷键 | 作用 |
|--------|------|
| `Enter` / `Cmd+Enter` | 焦点在输入框 → 发送（两者等效） |
| `Enter` | 焦点在 Diff 改动区 → 接受当前改动 |
| `Cmd+Enter`（Diff 焦点） | 无效（不发送、不接受） |
| `Cmd+N` | 新任务 |
| `Cmd+E` | @引用当前文件 |
| `↑/↓` | Diff：上/下一个改动 |
| `Delete` | Diff：拒绝当前改动 |
| `Space` | Diff：打开/发送批注 |
| `←/→` | Diff：上/下一个文件 |
| `Esc` | 关闭当前面板 / 取消当前操作 |
| `Cmd+,` | 打开设置 |

---

## 七、搭档的语气和措辞

### 7.1 常用句式

| 场景 | 搭档说 |
|------|--------|
| 改动暂存 | "改动已暂存，等你确认写入。" |
| 任务完成（写入）| "✅ 改动已写入文件。" |
| 任务完成（解决确认）| "我完成了，附上验证证据——你确认一下问题是否解决（交付≠解决，以你确认关闭为准）。" |
| 主动提醒 | 默认不说话。用户可在设置开启「长时间不动时提醒」 |
| API Key 失效 | "API Key 好像失效了。要不要更新一下？" |

其他句式已整合到本章各节。

### 7.2 搭档不说什么

新增：❌ 不用 emoji（✅、⚠️、💡 除外，不用 🎉😂😊👍）

---

## 八、视觉设计方向

V1 仅深色主题。详细色值见 `product/03-design-tokens.md`。屏幕级视觉见 `product/05-visual-spec.md`。

---

## 九、设置（V1 最小集）

```
设置
─────────────────

搭档
  语言
  ● 中文  ○ English

  默认视图
  ● 代码视图  ○ 页面视图

  长时间不动时提醒
  ○ 开启  ● 关闭
```

---

## 十、非功能规格

已整合到本章各节。

---

## 十一、版本范围

**V1 包含：**

- 首次配置页（API Key）
- 启动页（场景卡片——整理文件/做小工具/修系统/0-1 交付）
- 主工作区（问题台账 + 对话区 + 产物区交付包）
- 问题闭环流（DoD 对齐（DoD 对齐 ⏸ V2——2026-08-15 裁决；V1 以解决确认卡（证据对账）+交付包为可验证闭环） → 执行 → 交付包验收 → 确认关闭）
- 交付包（产物 + 验收对照逐项打勾 + 复跑入口 + 不装能指导）
- 问题台账（7 态状态机 / 断点续做（V1 限制：消息+台账恢复、状态机不跨重启——V2 会话快照，2026-08-15 裁决） / 复开）
- 0-1 交付流（无阶段确认驱动——目标确认 → 方案批准（PlanProposal）→ 解决确认（证据对账）——无固定阶段；分步确认/迭代可 demo）
- 数字产物交付（文件整理/合并/转换/清洗 + 变更预览 + L3 授权）
- 信任阶梯（L1-L4 渐进授权 + 快照回滚 + 委托规则）
- @文件引用 + 搭档须知（.neonforge）
- 设置（语言 / 默认视图 / 提醒开关）+ 内置插件注册（无市场）
- 深色主题（仅深色）

**V1 明确不做：**

- Inline 代码补全
- 多 Agent 并行（Worktree 隔离）
- 插件市场
- Git GUI
- 社交/协作
- 闲聊/画图
- Web 版
- 浅色主题
- 多语言（只做中文和 English）
